"""AlphaZero-style training for Santorini: MCTS-guided self-play with a
policy+value network over a fast numpy game core.

The PPO pipeline in the package root is untouched and stays as a baseline.
"""
